{
  "name": "lambda-daily",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "prepare-zip": "npm run build && cd dist && zip -r ../../daily.zip ."
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/node": "^20.19.14",
    "typescript": "^5.6.2"
  }
}
